device_type: SHRGBW2
id: SHRGBW2
name: Shelly RGBW2
generation: devicegeneration.gen1
apis:
  settings:
    description: 'Gen1 API endpoint: settings'
    response_structure:
      device:
        type: string
        mac: string
        hostname: string
        num_outputs: integer
        mode: string
      wifi_ap:
        enabled: boolean
        ssid: string
        key: string
      wifi_sta:
        enabled: boolean
        ssid: string
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      wifi_sta1:
        enabled: boolean
        ssid: 'null'
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      ap_roaming:
        enabled: boolean
        threshold: integer
      mqtt:
        enable: boolean
        server: string
        user: string
        id: string
        reconnect_timeout_max: float
        reconnect_timeout_min: float
        clean_session: boolean
        keep_alive: integer
        max_qos: integer
        retain: boolean
        update_period: integer
      coiot:
        enabled: boolean
        update_period: integer
        peer: string
        execute_enable: boolean
      sntp:
        server: string
        enabled: boolean
      login:
        enabled: boolean
        unprotected: boolean
        username: string
      pin_code: string
      name: string
      fw: string
      factory_reset_from_switch: boolean
      pon_wifi_reset: boolean
      discoverable: boolean
      build_info:
        build_id: string
        build_timestamp: string
        build_version: string
      cloud:
        enabled: boolean
        connected: boolean
      timezone: string
      lat: float
      lng: float
      tzautodetect: boolean
      tz_utc_offset: integer
      tz_dst: boolean
      tz_dst_auto: boolean
      time: string
      unixtime: integer
      led_status_disable: boolean
      debug_enable: boolean
      allow_cross_origin: boolean
      fw_mode: string
      actions:
        active: boolean
        names:
        - '...'
      hwinfo:
        hw_revision: string
        batch_id: integer
      mode: string
      alt_modes:
      - string
      dcpower: integer
      lights:
      - name: '...'
        ison: '...'
        red: '...'
        green: '...'
        blue: '...'
        white: '...'
        gain: '...'
        transition: '...'
        effect: '...'
        default_state: '...'
        auto_on: '...'
        auto_off: '...'
        schedule: '...'
        btn_type: '...'
        btn_reverse: '...'
        night_mode: '...'
        schedule_rules: '...'
      night_mode:
        enabled: boolean
        start_time: string
        end_time: string
        brightness: integer
      eco_mode_enabled: boolean
  status:
    description: 'Gen1 API endpoint: status'
    response_structure:
      wifi_sta:
        connected: boolean
        ssid: string
        ip: string
        rssi: integer
      cloud:
        enabled: boolean
        connected: boolean
      mqtt:
        connected: boolean
      time: string
      unixtime: integer
      serial: integer
      has_update: boolean
      mac: string
      cfg_changed_cnt: integer
      actions_stats:
        skipped: integer
      mode: string
      input: integer
      lights:
      - ison: '...'
        source: '...'
        has_timer: '...'
        timer_started: '...'
        timer_duration: '...'
        timer_remaining: '...'
        mode: '...'
        red: '...'
        green: '...'
        blue: '...'
        white: '...'
        gain: '...'
        effect: '...'
        transition: '...'
        power: '...'
        overpower: '...'
      meters:
      - power: '...'
        is_valid: '...'
        overpower: '...'
        timestamp: '...'
        counters: '...'
        total: '...'
      inputs:
      - input: '...'
        event: '...'
        event_cnt: '...'
      update:
        status: string
        has_update: boolean
        new_version: string
        old_version: string
        beta_version: string
      ram_total: integer
      ram_free: integer
      fs_size: integer
      fs_free: integer
      uptime: integer
  shelly:
    description: 'Gen1 API endpoint: shelly'
    response_structure:
      type: string
      mac: string
      auth: boolean
      fw: string
      discoverable: boolean
      num_outputs: integer
      mode: string
  settings/actions:
    description: 'Gen1 API endpoint: settings/actions'
    response_structure:
      actions:
        btn_on_url:
        - '...'
        btn_off_url:
        - '...'
        out_on_url:
        - '...'
        out_off_url:
        - '...'
        longpush_url:
        - '...'
        shortpush_url:
        - '...'
  settings/ap:
    description: 'Gen1 API endpoint: settings/ap'
    response_structure:
      enabled: boolean
      ssid: string
      key: string
  settings/light:
    description: 'Gen1 API endpoint: settings/light'
    response_structure:
      device:
        type: string
        mac: string
        hostname: string
        num_outputs: integer
        mode: string
      wifi_ap:
        enabled: boolean
        ssid: string
        key: string
      wifi_sta:
        enabled: boolean
        ssid: string
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      wifi_sta1:
        enabled: boolean
        ssid: 'null'
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      ap_roaming:
        enabled: boolean
        threshold: integer
      mqtt:
        enable: boolean
        server: string
        user: string
        id: string
        reconnect_timeout_max: float
        reconnect_timeout_min: float
        clean_session: boolean
        keep_alive: integer
        max_qos: integer
        retain: boolean
        update_period: integer
      coiot:
        enabled: boolean
        update_period: integer
        peer: string
        execute_enable: boolean
      sntp:
        server: string
        enabled: boolean
      login:
        enabled: boolean
        unprotected: boolean
        username: string
      pin_code: string
      name: string
      fw: string
      factory_reset_from_switch: boolean
      pon_wifi_reset: boolean
      discoverable: boolean
      build_info:
        build_id: string
        build_timestamp: string
        build_version: string
      cloud:
        enabled: boolean
        connected: boolean
      timezone: string
      lat: float
      lng: float
      tzautodetect: boolean
      tz_utc_offset: integer
      tz_dst: boolean
      tz_dst_auto: boolean
      time: string
      unixtime: integer
      led_status_disable: boolean
      debug_enable: boolean
      allow_cross_origin: boolean
      fw_mode: string
      actions:
        active: boolean
        names:
        - '...'
      hwinfo:
        hw_revision: string
        batch_id: integer
      mode: string
      alt_modes:
      - string
      dcpower: integer
      lights:
      - name: '...'
        ison: '...'
        red: '...'
        green: '...'
        blue: '...'
        white: '...'
        gain: '...'
        transition: '...'
        effect: '...'
        default_state: '...'
        auto_on: '...'
        auto_off: '...'
        schedule: '...'
        btn_type: '...'
        btn_reverse: '...'
        night_mode: '...'
        schedule_rules: '...'
      night_mode:
        enabled: boolean
        start_time: string
        end_time: string
        brightness: integer
      eco_mode_enabled: boolean
  settings/login:
    description: 'Gen1 API endpoint: settings/login'
    response_structure:
      enabled: boolean
      unprotected: boolean
      username: string
  settings/mqtt:
    description: 'Gen1 API endpoint: settings/mqtt'
    response_structure:
      device:
        type: string
        mac: string
        hostname: string
        num_outputs: integer
        mode: string
      wifi_ap:
        enabled: boolean
        ssid: string
        key: string
      wifi_sta:
        enabled: boolean
        ssid: string
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      wifi_sta1:
        enabled: boolean
        ssid: 'null'
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      ap_roaming:
        enabled: boolean
        threshold: integer
      mqtt:
        enable: boolean
        server: string
        user: string
        id: string
        reconnect_timeout_max: float
        reconnect_timeout_min: float
        clean_session: boolean
        keep_alive: integer
        max_qos: integer
        retain: boolean
        update_period: integer
      coiot:
        enabled: boolean
        update_period: integer
        peer: string
        execute_enable: boolean
      sntp:
        server: string
        enabled: boolean
      login:
        enabled: boolean
        unprotected: boolean
        username: string
      pin_code: string
      name: string
      fw: string
      factory_reset_from_switch: boolean
      pon_wifi_reset: boolean
      discoverable: boolean
      build_info:
        build_id: string
        build_timestamp: string
        build_version: string
      cloud:
        enabled: boolean
        connected: boolean
      timezone: string
      lat: float
      lng: float
      tzautodetect: boolean
      tz_utc_offset: integer
      tz_dst: boolean
      tz_dst_auto: boolean
      time: string
      unixtime: integer
      led_status_disable: boolean
      debug_enable: boolean
      allow_cross_origin: boolean
      fw_mode: string
      actions:
        active: boolean
        names:
        - '...'
      hwinfo:
        hw_revision: string
        batch_id: integer
      mode: string
      alt_modes:
      - string
      dcpower: integer
      lights:
      - name: '...'
        ison: '...'
        red: '...'
        green: '...'
        blue: '...'
        white: '...'
        gain: '...'
        transition: '...'
        effect: '...'
        default_state: '...'
        auto_on: '...'
        auto_off: '...'
        schedule: '...'
        btn_type: '...'
        btn_reverse: '...'
        night_mode: '...'
        schedule_rules: '...'
      night_mode:
        enabled: boolean
        start_time: string
        end_time: string
        brightness: integer
      eco_mode_enabled: boolean
  settings/network:
    description: 'Gen1 API endpoint: settings/network'
    response_structure:
      device:
        type: string
        mac: string
        hostname: string
        num_outputs: integer
        mode: string
      wifi_ap:
        enabled: boolean
        ssid: string
        key: string
      wifi_sta:
        enabled: boolean
        ssid: string
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      wifi_sta1:
        enabled: boolean
        ssid: 'null'
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      ap_roaming:
        enabled: boolean
        threshold: integer
      mqtt:
        enable: boolean
        server: string
        user: string
        id: string
        reconnect_timeout_max: float
        reconnect_timeout_min: float
        clean_session: boolean
        keep_alive: integer
        max_qos: integer
        retain: boolean
        update_period: integer
      coiot:
        enabled: boolean
        update_period: integer
        peer: string
        execute_enable: boolean
      sntp:
        server: string
        enabled: boolean
      login:
        enabled: boolean
        unprotected: boolean
        username: string
      pin_code: string
      name: string
      fw: string
      factory_reset_from_switch: boolean
      pon_wifi_reset: boolean
      discoverable: boolean
      build_info:
        build_id: string
        build_timestamp: string
        build_version: string
      cloud:
        enabled: boolean
        connected: boolean
      timezone: string
      lat: float
      lng: float
      tzautodetect: boolean
      tz_utc_offset: integer
      tz_dst: boolean
      tz_dst_auto: boolean
      time: string
      unixtime: integer
      led_status_disable: boolean
      debug_enable: boolean
      allow_cross_origin: boolean
      fw_mode: string
      actions:
        active: boolean
        names:
        - '...'
      hwinfo:
        hw_revision: string
        batch_id: integer
      mode: string
      alt_modes:
      - string
      dcpower: integer
      lights:
      - name: '...'
        ison: '...'
        red: '...'
        green: '...'
        blue: '...'
        white: '...'
        gain: '...'
        transition: '...'
        effect: '...'
        default_state: '...'
        auto_on: '...'
        auto_off: '...'
        schedule: '...'
        btn_type: '...'
        btn_reverse: '...'
        night_mode: '...'
        schedule_rules: '...'
      night_mode:
        enabled: boolean
        start_time: string
        end_time: string
        brightness: integer
      eco_mode_enabled: boolean
  settings/relay/0:
    description: 'Gen1 API endpoint: settings/relay/0'
    response_structure:
      device:
        type: string
        mac: string
        hostname: string
        num_outputs: integer
        mode: string
      wifi_ap:
        enabled: boolean
        ssid: string
        key: string
      wifi_sta:
        enabled: boolean
        ssid: string
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      wifi_sta1:
        enabled: boolean
        ssid: 'null'
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      ap_roaming:
        enabled: boolean
        threshold: integer
      mqtt:
        enable: boolean
        server: string
        user: string
        id: string
        reconnect_timeout_max: float
        reconnect_timeout_min: float
        clean_session: boolean
        keep_alive: integer
        max_qos: integer
        retain: boolean
        update_period: integer
      coiot:
        enabled: boolean
        update_period: integer
        peer: string
        execute_enable: boolean
      sntp:
        server: string
        enabled: boolean
      login:
        enabled: boolean
        unprotected: boolean
        username: string
      pin_code: string
      name: string
      fw: string
      factory_reset_from_switch: boolean
      pon_wifi_reset: boolean
      discoverable: boolean
      build_info:
        build_id: string
        build_timestamp: string
        build_version: string
      cloud:
        enabled: boolean
        connected: boolean
      timezone: string
      lat: float
      lng: float
      tzautodetect: boolean
      tz_utc_offset: integer
      tz_dst: boolean
      tz_dst_auto: boolean
      time: string
      unixtime: integer
      led_status_disable: boolean
      debug_enable: boolean
      allow_cross_origin: boolean
      fw_mode: string
      actions:
        active: boolean
        names:
        - '...'
      hwinfo:
        hw_revision: string
        batch_id: integer
      mode: string
      alt_modes:
      - string
      dcpower: integer
      lights:
      - name: '...'
        ison: '...'
        red: '...'
        green: '...'
        blue: '...'
        white: '...'
        gain: '...'
        transition: '...'
        effect: '...'
        default_state: '...'
        auto_on: '...'
        auto_off: '...'
        schedule: '...'
        btn_type: '...'
        btn_reverse: '...'
        night_mode: '...'
        schedule_rules: '...'
      night_mode:
        enabled: boolean
        start_time: string
        end_time: string
        brightness: integer
      eco_mode_enabled: boolean
  settings/cloud:
    description: 'Gen1 API endpoint: settings/cloud'
    response_structure:
      enabled: boolean
  settings/device:
    description: 'Gen1 API endpoint: settings/device'
    response_structure:
      device:
        type: string
        mac: string
        hostname: string
        num_outputs: integer
        mode: string
      wifi_ap:
        enabled: boolean
        ssid: string
        key: string
      wifi_sta:
        enabled: boolean
        ssid: string
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      wifi_sta1:
        enabled: boolean
        ssid: 'null'
        ipv4_method: string
        ip: 'null'
        gw: 'null'
        mask: 'null'
        dns: 'null'
      ap_roaming:
        enabled: boolean
        threshold: integer
      mqtt:
        enable: boolean
        server: string
        user: string
        id: string
        reconnect_timeout_max: float
        reconnect_timeout_min: float
        clean_session: boolean
        keep_alive: integer
        max_qos: integer
        retain: boolean
        update_period: integer
      coiot:
        enabled: boolean
        update_period: integer
        peer: string
        execute_enable: boolean
      sntp:
        server: string
        enabled: boolean
      login:
        enabled: boolean
        unprotected: boolean
        username: string
      pin_code: string
      name: string
      fw: string
      factory_reset_from_switch: boolean
      pon_wifi_reset: boolean
      discoverable: boolean
      build_info:
        build_id: string
        build_timestamp: string
        build_version: string
      cloud:
        enabled: boolean
        connected: boolean
      timezone: string
      lat: float
      lng: float
      tzautodetect: boolean
      tz_utc_offset: integer
      tz_dst: boolean
      tz_dst_auto: boolean
      time: string
      unixtime: integer
      led_status_disable: boolean
      debug_enable: boolean
      allow_cross_origin: boolean
      fw_mode: string
      actions:
        active: boolean
        names:
        - '...'
      hwinfo:
        hw_revision: string
        batch_id: integer
      mode: string
      alt_modes:
      - string
      dcpower: integer
      lights:
      - name: '...'
        ison: '...'
        red: '...'
        green: '...'
        blue: '...'
        white: '...'
        gain: '...'
        transition: '...'
        effect: '...'
        default_state: '...'
        auto_on: '...'
        auto_off: '...'
        schedule: '...'
        btn_type: '...'
        btn_reverse: '...'
        night_mode: '...'
        schedule_rules: '...'
      night_mode:
        enabled: boolean
        start_time: string
        end_time: string
        brightness: integer
      eco_mode_enabled: boolean
parameters:
  name:
    type: string
    description: Device name
    api: settings
    parameter_path: name
  mqtt_enable:
    type: boolean
    description: Enable MQTT
    api: settings
    parameter_path: mqtt.enable
  mqtt_server:
    type: string
    description: MQTT server address
    api: settings
    parameter_path: mqtt.server
type_mappings:
- SHRGBW2
